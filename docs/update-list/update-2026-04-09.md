# 更新详情 - 2026-04-09

## 概述

**更新时间**: 2026-04-09
**触发方式**: 版本发布 1.3.0
**关联提交**: d4a78f3, a4339a0, 422d952, 5a6d458

## 文档变更详情

### CHANGELOG.md

**变更类型**: 版本更新

**变更内容**:
- 新增 `[1.3.0] - 2026-04-09` 版本条目
- 记录 Toast 重构、GsonUtils 新增、包路径迁移等变更
- 更新 Version History Summary 表格
- 新增 1.3.0 底部链接

### PROJECT_OVERVIEW.md

**变更类型**: 版本更新 + 内容更新

**变更内容**:
- 当前版本从 1.2.9 更新为 1.3.0
- common-core 模块说明增加 GsonUtils，依赖增加 Gson
- common-ui 模块说明增加 Toaster 依赖

### DEPENDENCIES.md

**变更类型**: 新增依赖

**变更内容**:
- 新增 Gson-Factory 5.0.0 依赖
- 新增 Toaster 13.5 依赖

### api/common-core.md

**变更类型**: 新增 API

**变更内容**:
- 新增 `com.hoyn.common.core.gson` 包文档
- 新增 `GsonUtils` 工具类文档（方法列表、使用示例）
- 新增 `GsonParseExceptionHandler` SAM 接口文档
- 新增 `GsonParseExceptionEvent` 数据类文档
- 版本引用更新为 1.3.0

### api/common-ui.md

**变更类型**: 新增 API

**变更内容**:
- 新增 `ToastUtils` 工具类文档（方法列表、使用示例）
- 新增 `ToastConfig` 配置类文档
- 新增 Toaster 依赖声明

### checklist/modules.md

**变更类型**: 版本更新 + 内容更正

**变更内容**:
- 所有依赖版本号从 1.2.7 更新为 1.3.0
- JSON 解析模块归属从 `common-utils` 更正为 `common-core`
- JVM Target 从 11 更正为 17
- 更新日志版本更新为 1.3.0

## Git 提交详细分析

### d4a78f3 - 重构Toast

**变动文件**:
- `common-ui/toast/` 新增 6 个文件（ToastUtils、ToastConfig、ToastCallerLocator、ToastInterceptorComposer、ToastParamsMapper、ToastStackSkipsStore）
- `common-ui/toast/inner/` 删除整个旧实现目录
- `common-utils/ToastUtils.kt` 删除（迁移至 common-ui）
- `BaseActivity.kt` / `BaseFragment.kt` / `BaseDialogFragment.kt` toast 引用更新为新包路径
- `MainApplication.kt` ToastUtils import 更新
- `ToastDemoActivity.kt` Toast Demo 更新适配新 API
- 各模块 `build.gradle.kts` 依赖更新

### a4339a0 - feat: add APK build info task

**变动文件**:
- `app/build.gradle.kts` 新增 APK 构建信息打印任务

### 422d952 - 删除workflow

**变动文件**:
- 删除 GitHub workflow 文件

### 5a6d458 - update github workflow

**变动文件**:
- GitHub workflow 更新
