<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Commonlib">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Demo Activities -->
        <activity android:name=".ui.compose.ComposeDemoActivity" />
        <activity android:name=".ui.toast_demo.ToastDemoActivity" />
        <activity android:name=".ui.network.NetworkDemoActivity" />
        <activity android:name=".ui.mmkv_demo.MmkvDemoActivity" />
        <activity android:name=".ui.log_demo.LogDemoActivity" />
        <activity android:name=".ui.statusbar_demo.StatusBarDemoActivity" />
        <activity android:name=".ui.liveevent.LiveEventDemoActivity" />
        <service
            android:name=".ui.liveevent.LiveEventDemoService"
            android:exported="false" />
        <receiver
            android:name=".ui.liveevent.LiveEventDemoReceiver"
            android:exported="false" />

    </application>

</manifest>