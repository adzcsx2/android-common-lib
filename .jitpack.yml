# JitPack 配置文件
# 用于支持多模块项目的独立发布

jdk:
  - openjdk17

install:
  # 发布所有模块到本地 Maven 仓库
  # -x test -x lint: 跳过测试和 lint 检查
  # --parallel: 并行构建多模块
  - ./gradlew publishToMavenLocal -x test -x lint --parallel

